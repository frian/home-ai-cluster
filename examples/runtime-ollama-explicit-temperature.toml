runtime = "ollama"
# Omission preserves native sampling; this illustrative explicit value is an
# operator choice, not a HAC recommendation or default.
temperature = 0.2

[ollama]
# Replace with an already-installed operator-owned Ollama model.
model = "replace-with-installed-model"
disable_thinking = true
