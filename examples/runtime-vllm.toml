runtime = "vllm"

[vllm]
# Adapt this loopback port to the already-running local vLLM server.
base_url = "http://127.0.0.1:8000"
# Replace with that server's served model identifier.
model = "replace-with-served-model"
